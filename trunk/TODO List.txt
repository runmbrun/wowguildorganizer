A List of all the feature and bug fixes that will need to be incorportated at some point in time.  
In no particular order...


=====================
+ Updates and Fixes +
=====================

Global:
1.  ADD: dual spec functionality to the GuildMember class. 
	a. Would be able to switch back and forth as needed. (Right click functionality)
2.  FIX: Audit - Currently the check for empty gem slots is not working.
3.  ADD: Tooltips - Make sure in an audit, that the correct stats are displayed, not just the base stats.
4.  ADD: Items - pvp gear isn't using contexts correctly.  
	a. Example: http://us.battle.net/api/wow/item/119831
		i. Shows up in Character as vendor instead of {"id":119831,"availableContexts":["pvp-unranked"]}
5.  ADD: Need functionality to parse wowhead.com to get boss loot table.
	b. Make sure old raids can be hidden (visible=true), or deleted.
6.  ADD: Finish Tier 17 bosses and loot tables...
7.  FIX: Make sure that the Refresh function removes guildies that have quit the guild.
8.  ADD: All crafted items aren't displaying properly.  Need to calculate suffixes and upgrade level.
9.  FIX: Slot #21 Error

